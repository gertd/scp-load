{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "cwd": "${workspaceFolder}",
            "env": {},
            "args": [
                "/home/gertd/workspace/scratch/elastic/Employees100K.json",
                "--tenant", "${env:SCP_TENANT}"
            ]
        }
    ]
}